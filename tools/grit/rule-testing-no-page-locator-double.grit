`$page.$method("[data-testid=$id]")` as $node where {  $method <: `locator`,
  register_diagnostic($method, message="[test/no-testid-selectors] Prefer page. Hint: reach for getByRole('button', { name: 'Save' }) or getByLabelText('Email') before falling back to data-testid.getByRole/Label/Placeholder/Alt/Title. Using data-testid requires an ACCESSIBILITY justification.")
}
