`<$_ className="$class" aria-selected=$state $attrs>$body</$_>` as $node where {
  register_diagnostic(span=$node, message="Stateful slot detected. Extract a semantic class (e.g. .tabs__trigger) with explicit state styling.")
}
