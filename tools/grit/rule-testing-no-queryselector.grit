`$recv.$method($sel)` as $node where {
  $method <: `querySelector`,
  register_diagnostic(
    $node,
    message = "[test/no-queryselector] Avoid CSS selectors in tests; prefer a11y queries (ByRole/Label/Placeholder/Alt/Title)."
  )
}
