`document.$method($cls)` as $node where {
  $method <: `getElementsByClassName`,
  register_diagnostic(
    $node,
    message = "[test/no-raw-dom-lookup] Prefer accessible queries over raw DOM lookups in tests."
  )
}
