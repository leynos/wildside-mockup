`$recv.$method($arg)` as $node where {  $method <: `queryByTestId`,
  register_diagnostic($method, message="[test/no-testid-selectors] Prefer accessible queries. Hint: reach for getByRole('button', { name: 'Save' }) or getByLabelText('Email') before falling back to data-testid. *ByTestId requires an ACCESSIBILITY justification.")
}
