`<nav $navAttrs>...<a className="$class" $attrs>$inner</a>...</nav>` as $node where {
  register_diagnostic(span=$node, message="Navigation link candidate found. Extract a semantic class (e.g. .nav__link) instead of inline utilities.")
}
