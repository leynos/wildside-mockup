`<h4 className="$class" $attrs>$body</h4>` as $node where {
  register_diagnostic(span=$node, message="Heading uses inline utilities. Extract a semantic title class (e.g. .section-title) and reuse.")
}
