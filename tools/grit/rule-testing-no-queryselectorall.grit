`$recv.$method($sel)` as $node where {
  $method <: `querySelectorAll`,
  register_diagnostic(
    $node,
    message = "[test/no-queryselector] Avoid CSS selectors in tests; prefer a11y queries."
  )
}
