`cy.$method("[data-testid=$id]")` as $node where {  $method <: `get`,
  register_diagnostic($method, message="[test/no-testid-selectors] Prefer a11y locators (testing-library/cypress or cy. Hint: reach for getByRole('button', { name: 'Save' }) or getByLabelText('Email') before falling back to data-testid.findByRole). data-testid requires an ACCESSIBILITY justification.")
}
